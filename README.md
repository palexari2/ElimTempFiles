I COMPLETELY DON'T KNOW WHY BUT THE ANTIVIRUS KEEPS FLASHING IT AS A TROJAN. DON'T WORRY, IT'S NOT.

Hi, thanks for downloading Palexari12's temp files remover!

Don't worry about windows blocking the elimination or if the script eliminates the files you're using: it won't!

config.txt is the file where you can configure the script. Read carefully!

How does this work? The central part is the script. It will create a task in your task scheduler (it is optional, you can deactivate it in "config.txt", it is on by default) that will eliminate your temp files every 3 days. IF you enable "deep_cleaning" in "config.txt" writing "True" where is written "False", the script will ask admin permissions. Because not many people like that, it's optional and default to "False". Anyway, IF you enable it, after you give it admin permissions it will eliminate windows' temporary files, too.
So. think about it!
